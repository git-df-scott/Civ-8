/**
 * @civ8/ui-components — shared React components (menus, panels, tech tree).
 *
 * M0 placeholder: React 19 lands here when the HUD does (M3+).
 */

export const UI_COMPONENTS_PLACEHOLDER = true as const;
