/**
 * @civ8/content — content schemas (Zod) and the base-game content pack.
 *
 * M0 placeholder: schemas land in M1+, the base pack from M3 onward.
 */

export const CONTENT_PACKAGE_PLACEHOLDER = true as const;
